"""
DEPRECATED: This module was superseded by the `accounting/urls/` package (i.e. the
`accounting.urls` package defined in `accounting/urls/__init__.py`).

This shim intentionally exposes an empty `urlpatterns` list so that legacy imports
do not register duplicate routes.
"""

app_name = "accounting"

urlpatterns = []
